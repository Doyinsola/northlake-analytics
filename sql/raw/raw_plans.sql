CREATE OR REPLACE TABLE NORTHLAKE_DB.RAW.raw_plans(
    plan_id VARCHAR, 
    plan_name VARCHAR, 
    billing_cycle VARCHAR, 
    mrr_usd VARCHAR, 
    max_seats VARCHAR, 
    max_api_calls VARCHAR
);